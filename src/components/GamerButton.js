import React from 'react'
import { Button } from 'semantic-ui-react'

class GamerButton extends React.Component{
    render(){
        return(
            <Button>What's up gamers</Button>
        )
    }
}

export default GamerButton
